{% extends 'base.html.twig' %}

{% block title %}Offers{% endblock %}

{% block body %}
    <h1>Offers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offer.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_offers_index') }}">back to list</a>

    <a href="{{ path('app_admin_offers_edit', {'id': offer.id}) }}">edit</a>

    {{ include('admin/offers/_delete_form.html.twig') }}
{% endblock %}
