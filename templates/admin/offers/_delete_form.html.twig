<form class="text-end"  method="post" action="{{ path('app_admin_offers_delete', {'id': offer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offer.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>

</form>

