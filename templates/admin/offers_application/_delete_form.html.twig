<form method="post" action="{{ path('app_admin_offers_application_delete', {'id': offers_application.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offers_application.id) }}">
    <button class="btn">Delete</button>
</form>
