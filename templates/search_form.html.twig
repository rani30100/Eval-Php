{{ form_start(search_form) }}
<div class="d-flex justify-content-center align-items-center gap-3">
	{{ form_row(search_form.search,{ 'attr' : { 'class': 'border border-2 bg-primary-subtle hover' }})}}
	{{ form_row(search_form.submit) }}
</div>
<div class="d-flex justify-content-center align-items-center gap-3">
	{{ form_row(search_form.type, { 'attr' : { 'class': 'border border-2 bg-primary-subtle hover' } }) }}
	{{ form_row(search_form.department, { 'attr' : { 'class': 'border border-2 bg-primary-subtle hover' } }) }}

</div>
{{ form_end(search_form) }}
