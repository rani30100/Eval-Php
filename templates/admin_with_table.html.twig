{% extends 'admin.html.twig' %}

{% block body %}
    <div class="container">
        <div class="card shadow-lg">
            <h1 class="card-header text-center">{{ title|default('Liste') }}</h1>
            <div class="card-body">
                {% block actions %}
                {% endblock %}
                <div class="table-responsive">
                    <table class="table table-striped table-hover">
                        {% block table_content %}
                        {% endblock %}
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}